import { expect,test } from "@playwright/test";
const password=process.env.SEED_DEMO_PASSWORD!;
test("flujos principales son utilizables sin overflow en móvil",async({page})=>{await page.goto("/login");await page.getByLabel("Email").fill("miguel@murraydjs.local");await page.getByLabel("Contraseña").fill(password);await page.getByRole("button",{name:"Ingresar"}).click();await page.waitForURL("**/dashboard");for(const path of ["/clientes/nuevo","/presupuestos/nuevo","/presupuestos","/agenda?month=2026-12","/personal","/personal/liquidaciones?month=2026-12&currency=ARS","/gastos","/reportes/rentabilidad?month=12&year=2026&currency=ARS"]){await page.goto(path);expect(await page.evaluate(()=>document.documentElement.scrollWidth<=document.documentElement.clientWidth),`overflow en ${path}`).toBe(true);}await page.goto("/presupuestos");await page.getByRole("link",{name:/PRE-/}).first().click();expect(await page.evaluate(()=>document.documentElement.scrollWidth<=document.documentElement.clientWidth)).toBe(true);const eventLink=page.getByRole("link",{name:"Ver evento"});if(await eventLink.isVisible()){await eventLink.click();expect(await page.evaluate(()=>document.documentElement.scrollWidth<=document.documentElement.clientWidth)).toBe(true);}});
