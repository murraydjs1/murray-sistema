import Link from "next/link"; import { prisma } from "@/server/db/prisma"; import { requireManagement } from "@/server/auth/authorization";
export default async function Clients(){await requireManagement();const clients=await prisma.client.findMany({include:{_count:{select:{quotes:true,events:true}}},orderBy:{name:"asc"}});return <><div className="topbar"><div><div className="eyebrow">Base de clientes</div><h1>Clientes</h1></div><Link className="btn btn-primary" href="/clientes/nuevo">+ Nuevo cliente</Link></div><div className="table-wrap"><table className="table"><thead><tr><th>Nombre</th><th>Tipo</th><th>Teléfono</th><th>Eventos</th></tr></thead><tbody>{clients.map(c=><tr key={c.id}><td><Link href={`/clientes/${c.id}`}>{c.name}</Link></td><td data-label="Tipo">{c.type}</td><td data-label="Teléfono">{c.phone||"—"}</td><td data-label="Eventos">{c._count.events}</td></tr>)}{!clients.length&&<tr><td colSpan={4} className="empty">Creá el primer cliente para empezar.</td></tr>}</tbody></table></div></>}
