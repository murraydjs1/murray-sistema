import { createCatalogItem, updateCatalogItem } from "@/app/actions/catalog"; import { prisma } from "@/server/db/prisma"; import { requireManagement } from "@/server/auth/authorization"; import { formatMoney } from "@/lib/money/format";
export default async function Catalog(){await requireManagement();const [services,addOns]=await Promise.all([prisma.service.findMany({orderBy:{name:"asc"}}),prisma.addOn.findMany({orderBy:{name:"asc"}})]);return <><div className="topbar"><div><div className="eyebrow">Precios base</div><h1>Servicios y adicionales</h1></div></div><section className="grid grid-2"><CatalogColumn title="Servicios" items={services} kind="service"/><CatalogColumn title="Adicionales" items={addOns} kind="addOn"/></section></>}
function CatalogColumn({title,items,kind}:{title:string;items:{id:string;name:string;category:string;description:string|null;listPrice:unknown;currency:"ARS"|"USD";active:boolean}[];kind:"service"|"addOn"}){return <div><div className="section-head"><h2>{title}</h2></div><div className="stack">{items.map(i=><details className="card catalog-item" key={i.id}><summary><span><strong>{i.name}</strong><small>{i.category}{!i.active?" · Inactivo":""}</small></span><strong>{formatMoney(String(i.listPrice),i.currency)}</strong></summary><form action={updateCatalogItem.bind(null,kind,i.id)} className="form edit-panel"><div className="form-grid"><div className="field"><label>Nombre</label><input name="name" defaultValue={i.name} required/></div><div className="field"><label>Categoría</label><input name="category" defaultValue={i.category} required/></div><div className="field"><label>Moneda</label><select name="currency" defaultValue={i.currency}><option>ARS</option><option>USD</option></select></div><div className="field"><label>Precio de lista</label><input name="listPrice" type="number" min="0" step="0.01" defaultValue={String(i.listPrice)} required/></div><div className="field span-2"><label>Descripción</label><textarea name="description" defaultValue={i.description||""}/></div></div><label className="check"><input type="checkbox" name="active" defaultChecked={i.active}/> Disponible para nuevos presupuestos</label><button className="btn btn-primary">Guardar cambios</button></form></details>)}</div><details className="card" style={{marginTop:12}}><summary style={{cursor:"pointer",fontWeight:700}}>+ Agregar {kind==="service"?"servicio":"adicional"}</summary><form action={createCatalogItem.bind(null,kind)} className="form" style={{marginTop:16}}><div className="field"><label>Nombre</label><input name="name" required/></div><div className="form-grid"><div className="field"><label>Categoría</label><input name="category" required/></div><div className="field"><label>Moneda</label><select name="currency"><option>ARS</option><option>USD</option></select></div><div className="field span-2"><label>Precio de lista</label><input name="listPrice" type="number" min="0" step="0.01" required/></div></div><button className="btn btn-primary">Guardar</button></form></details></div>}
