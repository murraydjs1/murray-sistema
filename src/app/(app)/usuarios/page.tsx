import { saveUser, toggleUser } from "@/app/actions/users"; import { prisma } from "@/server/db/prisma"; import { requireAdmin } from "@/server/auth/authorization";
export default async function Users(){await requireAdmin();const users=await prisma.user.findMany({orderBy:{name:"asc"}});return <><div className="topbar"><div><div className="eyebrow">Solo administradores</div><h1>Usuarios</h1></div></div><section className="grid grid-2"><div><div className="section-head"><h2>Equipo con acceso</h2></div><div className="stack">{users.map(u=><details className="card" key={u.id}><summary className="row space" style={{cursor:"pointer"}}><div><strong>{u.name}</strong><div className="muted">{u.email} · {u.role}</div></div><span className={`badge ${u.active?"badge-green":""}`}>{u.active?"Activo":"Inactivo"}</span></summary><form action={saveUser} className="form" style={{marginTop:18}}><input type="hidden" name="id" value={u.id}/><div className="field"><label>Nombre</label><input name="name" defaultValue={u.name} required/></div><div className="field"><label>Email</label><input name="email" type="email" defaultValue={u.email} required/></div><div className="field"><label>Rol</label><select name="role" defaultValue={u.role}><option>ADMIN</option><option>ADMIN_FINANCIERO</option><option>STAFF</option></select></div><div className="field"><label>Nueva contraseña (opcional)</label><input name="password" type="password" minLength={12}/></div><div className="row"><button className="btn btn-primary">Guardar cambios</button></div></form><form action={toggleUser.bind(null,u.id)} style={{marginTop:10}}><button className="btn btn-secondary">{u.active?"Desactivar":"Activar"}</button></form></details>)}</div></div><div><div className="section-head"><h2>Crear usuario</h2></div><form action={saveUser} className="card form"><div className="field"><label>Nombre *</label><input name="name" required/></div><div className="field"><label>Email *</label><input name="email" type="email" required/></div><div className="field"><label>Rol *</label><select name="role"><option value="ADMIN_FINANCIERO">ADMIN FINANCIERO</option><option value="STAFF">STAFF</option><option value="ADMIN">ADMIN</option></select></div><div className="field"><label>Contraseña inicial *</label><input name="password" type="password" minLength={12} required/><small className="muted">Mínimo 12 caracteres</small></div><button className="btn btn-primary">Crear usuario</button></form></div></section></>}
