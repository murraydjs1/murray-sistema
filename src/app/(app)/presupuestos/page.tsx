import Link from "next/link";import { prisma } from "@/server/db/prisma";import { requireManagement } from "@/server/auth/authorization";import { formatMoney } from "@/lib/money/format";
export default async function Quotes(){await requireManagement();const quotes=await prisma.quote.findMany({include:{client:true,versions:{orderBy:{versionNumber:"desc"},take:1}},orderBy:{createdAt:"desc"}});return <><div className="topbar"><div><div className="eyebrow">Comercial</div><h1>Presupuestos</h1></div><Link className="btn btn-primary" href="/presupuestos/nuevo">+ Nuevo presupuesto</Link></div><div className="table-wrap"><table className="table"><thead><tr><th>Número</th><th>Cliente</th><th>Evento</th><th>Estado</th><th>Último total</th></tr></thead><tbody>{quotes.map(q=>{const v=q.versions[0];return <tr key={q.id}><td><Link href={`/presupuestos/${q.id}`}>{q.number}</Link></td><td data-label="Cliente">{q.client.name}</td><td data-label="Evento">{q.eventDate.toLocaleDateString("es-AR",{timeZone:"UTC"})}</td><td><span className="badge">{q.status.replaceAll("_"," ")}</span></td><td data-label="Total">{v?formatMoney(String(v.totalFinal),v.currency):"Sin versión"}</td></tr>})}{!quotes.length&&<tr><td colSpan={5} className="empty">Todavía no hay presupuestos.</td></tr>}</tbody></table></div></>}
