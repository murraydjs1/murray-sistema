import { prisma } from "@/server/db/prisma";import { requireManagement } from "@/server/auth/authorization";import { NewQuoteForm } from "@/components/quotes/new-quote-form";
export default async function NewQuote(){await requireManagement();const [clients,types]=await Promise.all([prisma.client.findMany({where:{active:true},orderBy:{name:"asc"},select:{id:true,name:true}}),prisma.eventType.findMany({where:{active:true},orderBy:{name:"asc"},select:{id:true,name:true}})]);return <><div className="topbar"><div><div className="eyebrow">Cliente + evento</div><h1>Nuevo presupuesto</h1><p className="muted">Elegí un cliente existente o crealo sin salir de este flujo.</p></div></div><NewQuoteForm clients={clients} eventTypes={types}/></>}
